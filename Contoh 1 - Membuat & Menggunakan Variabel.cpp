#include <iostream>
using namespace std;

int main() {
  int a = 2, b = 6, hasil = a + b;
  
  cout << "variabel a : " + std::to_string(a) <<endl;
  cout << "variabel b : " + std::to_string(b) <<endl;
  cout << "variabel hasil = a + b : " + std::to_string(hasil);
  
  return 0;
}
